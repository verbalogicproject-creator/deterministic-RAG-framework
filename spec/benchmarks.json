{
  "spec_version": 1,
  "description": "Measured results, each recorded with the command that produces it. Nothing in docs/ may state a number that does not appear here or is not computed from a built index at render time. A figure without a producer is the failure mode this project exists to remove.",
  "corpus": {
    "source": "claude-cookbook-kg.db",
    "producer": "drf build --source <source> --out index.db",
    "note": "266 nodes / 605 edges verified true against the source; the ingest drops 4 dangling edges and collapses 48 duplicate groups, so the index holds 553."
  },
  "reproducibility": {
    "producer": "drf bench repro --index a.db --compare b.db",
    "measured": "2026-08-08",
    "axes": {
      "in_process_repeats": 5,
      "subprocess_repeats": 3,
      "hash_seeds": [
        "0",
        "1",
        "12345"
      ],
      "independent_builds": 2
    },
    "cells": 28,
    "distinct_digests": 1,
    "mismatched_positions": 0,
    "discordant_pairs": 0,
    "length_delta": 0,
    "symmetric_difference": 0
  },
  "chaos_control": {
    "producer": "drf bench chaos --index a.db",
    "measured": "2026-08-08",
    "what_it_is": "The prior engine's ranking reproduced exactly: sort by score alone with no tiebreak, over candidates in arbitrary order. python_apps_hybrid_query.py:304 applied to the unordered LIMIT 100 at :342. Not synthetic noise - the implementation this framework replaced.",
    "why_it_exists": "Every metric scores 1.0 on the real pipeline, so perfect scores are compatible with a harness that measures nothing. This control is the only evidence the harness can report failure.",
    "runs": 5,
    "rows": [
      {
        "metric": "distinct_digests",
        "real": "1",
        "chaos": "5",
        "separates": "yes"
      },
      {
        "metric": "mismatched_positions",
        "real": "0",
        "chaos": "551",
        "separates": "yes"
      },
      {
        "metric": "discordant_pairs",
        "real": "0",
        "chaos": "3931",
        "separates": "yes"
      },
      {
        "metric": "exact_match_rate",
        "real": "1.0000",
        "chaos": "0.6196",
        "separates": "yes"
      },
      {
        "metric": "kendall_tau",
        "real": "1.0000",
        "chaos": "0.9761",
        "separates": "barely"
      },
      {
        "metric": "rbo",
        "real": "1.0000",
        "chaos": "0.9942",
        "separates": "barely"
      },
      {
        "metric": "jaccard",
        "real": "1.0000",
        "chaos": "1.0000",
        "separates": "NO"
      },
      {
        "metric": "overlap_coefficient",
        "real": "1.0000",
        "chaos": "1.0000",
        "separates": "NO"
      }
    ],
    "findings": [
      "Set-based metrics are blind to ordering non-determinism. Jaccard reports 1.0000 for a pipeline returning five different orderings in five runs.",
      "Kendall's Tau of 0.9761 and RBO of 0.9942 describe a provably non-deterministic pipeline. Rounded for a report they read as 0.98 and 0.99. This is the measured reason assertions in this project use exact integers."
    ]
  },
  "sensitivity": {
    "producer": "drf bench sensitivity --index a.db",
    "measured": "2026-08-08",
    "queries": 23,
    "rows": [
      {
        "setting": "ranking.b",
        "default": "0.75",
        "probe": "0.375",
        "reordered": "13"
      },
      {
        "setting": "ranking.k1",
        "default": "1.2",
        "probe": "0.6",
        "reordered": "9"
      },
      {
        "setting": "graph.max_depth",
        "default": "2",
        "probe": "3",
        "reordered": "2"
      },
      {
        "setting": "graph.seed_count",
        "default": "10",
        "probe": "20",
        "reordered": "2"
      }
    ],
    "finding": "Lexical parameters dominate; graph parameters are live but weak. seed_count 10 -> 11 reorders zero of 15 corpus queries - it moves best_depth for 7 of them but never enough to change what a user sees. CORRECTED 2026-08-08: 'graph parameters are live but weak' understated it. That probe nudged a parameter; ablating the signal entirely changes 2 of 23 queries and never above rank 19. The graph layer is inert, not weak. See graph_contribution."
  },
  "length_normalisation": {
    "producer": "python3 tools/measure_length_norm.py --index index.db",
    "measured": "2026-08-08",
    "what_it_tests": "The prior engine scored idf*(tf*k1)/(tf+k1) with k1=2.5 and no b or avgdl - BM25 with length normalisation switched off.",
    "strictly_ordered_pairs": 352,
    "discordant": 60,
    "discordant_pct": "17.0%",
    "tie_affected_pairs": 95,
    "tie_affected_pct": "21.3%",
    "queries_with_tied_top_set": "7/15",
    "top_sets_disjoint": "3/15",
    "mean_top_set_length_normalised": "15.9",
    "mean_top_set_length_flat": "24.9",
    "corpus_mean_length": "18.9",
    "direction_longer_shorter_equal": "9/0/6",
    "correction": "Earlier figures of 26.2% discordance and 'top-1 changed 9 of 15' were tiebreak-dependent and are withdrawn. 7 of 15 queries have exact ties in a top set; breaking them by lowest node id gives 9, by highest gives 4. Those numbers described BM25 plus an arbitrary convention. All figures here are defined only over strictly-ordered comparisons. The directional claim survived unchanged."
  },
  "graph": {
    "producer": "measured during M1.3, recorded in STATE.md",
    "measured": "2026-08-07",
    "bfs_depth2_10_seeds_ms": "0.149",
    "full_depth4_all_pairs_ms": "5",
    "forward_only_mean_reach": "7.2",
    "bidirectional_mean_reach": "23.3",
    "forward_only_dead_nodes": "81/266",
    "bidirectional_dead_nodes": "5/266",
    "finding": "Following outgoing edges only would leave 76 nodes with zero graph signal because of how a relation happened to be phrased. Directed cycles are present, so a visited set is mandatory."
  },
  "scope_limits": {
    "no_relevance_labels": "49 model-generated judgements over 7 queries now exist (stratum A). They are NOT human judgements, and the annotator authored the retrieval system - see retrieval_quality.annotator. The one quality measurement made from them is a FAIL against a margin declared in advance. No positive claim about retrieval quality is made anywhere in this repository.",
    "toy_corpus": "266 nodes is small. Every determinism metric is perfect here, which demonstrates determinism, not scalability.",
    "posting_union_does_not_scale": "Candidate generation has no truncation and degrades at scale. FTS5 with a mandatory ORDER BY rank plus deterministic pagination is the milestone 3 path.",
    "oov_yields_nothing": "An all-out-of-vocabulary query produces empty D and therefore no proposals. Correct under subordination; the fix, if wanted, is a Stage 1 fix such as character n-grams, never a neural one.",
    "no_quality_measurement": "What M1 proves is reproducibility and subordination. Recall, nDCG and MRR need labels. An unqualified 'all metrics 1.0' would be exactly the drift this framework exists to prevent.",
    "advisory_reach_bounds_evaluation": "Measured: |D| runs 0 to 147 across the 23-query set, so the depth at which the advisory layer can act at all is a property of the query, not of the corpus. A quality comparison at any depth <= |D| is guaranteed to show zero difference between provider on and off.",
    "graph_decision_is_underpowered": "SUPERSEDED 2026-08-08. The graph question was thought to need labels and to be stuck at a sample size of one. It was neither: ablation plus nuisance screening settle it with no labels at all. See graph_contribution. The lesson kept: 'needs labels' was an assumption, not a measurement, and it deferred an answerable question through an entire milestone.",
    "quality_evidence_is_underpowered": "7 queries with 1-7 candidates each. The per-query record against a relevance-blind control is 3 wins / 3 losses / 1 tie. Any aggregate quality figure from this label set is dominated by single-query variation and must not be quoted as a system property."
  },
  "advisory_horizon": {
    "producer": "drf eval invariance --index index.db",
    "measured": "2026-08-08",
    "what_it_is": "|D| per query - the number of authoritative results. Merge is append-only, so every metric computed at k <= |D| is byte-identical with the neural provider on and off.",
    "queries": 23,
    "prefixes_identical": 23,
    "prefixes_differing": 0,
    "horizon_min": 0,
    "horizon_max": 147,
    "queries_with_no_reachable_evaluated_depth": 7,
    "evaluated_depths": [
      1,
      5,
      10,
      20
    ],
    "finding": "The advisory layer's reach is inverse to lexical success. Where stage 1 returned 20 or more documents it is structurally silent at every evaluated depth; where it returned one or two, the advisory layer can act from depth 5 down. The neural layer can only speak where lexical retrieval did badly, and is provably mute where it did well - which is what append-only subordination means, stated as a measurement rather than a design intention.",
    "second_finding": "A reachable horizon is necessary but not sufficient. The three out-of-vocabulary queries have |D| = 0, so by horizon alone the advisory layer could occupy every position - and it proposes nothing, because anchor-mode search takes its anchors from D. Anchor starvation is a separate bound from the horizon, and a recall figure on those queries would measure the starvation rather than the provider.",
    "why_it_matters_for_M2": "A quality comparison at any depth <= |D| is guaranteed to show zero difference between provider on and off. Reporting that as a finding about neural retrieval would be a category error, and measuring the horizon first is what makes the error visible in advance."
  },
  "retrieval_quality": {
    "producer": "python3 tools/drf eval quality --index index.db",
    "measured": "2026-08-08",
    "labels_hash": "33d8e1beb4302bbc5c6ada98d97b9047ff84581b6552387676a8648e96c7a439",
    "judgements": 49,
    "queries": 7,
    "stratum": "A_advisory (|D| <= 7, the only queries the advisory layer can reach)",
    "annotator": "MODEL-GENERATED (Claude Opus 5), graded blind to rank. NOT human judgements. The annotator also authored the retrieval system, which is a real confound: relevance calls may correlate with what this ranking already surfaces. Blind grading removes position bias but not authorship bias.",
    "verdict": "FAIL at every depth against the margin declared on 2026-08-08, before any label existed.",
    "rows": [
      {
        "depth": 1,
        "system_ndcg": 0.8367,
        "oracle_ndcg": 1.0,
        "best_blind": "shuffle_3",
        "best_blind_ndcg": 0.7959,
        "margin": 0.0408,
        "required": 0.05,
        "clears": false,
        "per_query": "2W 1L 4T"
      },
      {
        "depth": 5,
        "system_ndcg": 0.8787,
        "oracle_ndcg": 0.9811,
        "best_blind": "id_order",
        "best_blind_ndcg": 0.8412,
        "margin": 0.0376,
        "required": 0.05,
        "clears": false,
        "per_query": "3W 3L 1T"
      },
      {
        "depth": 10,
        "system_ndcg": 0.9064,
        "oracle_ndcg": 0.9787,
        "best_blind": "id_order",
        "best_blind_ndcg": 0.8761,
        "margin": 0.0303,
        "required": 0.05,
        "clears": false,
        "per_query": "3W 3L 1T"
      },
      {
        "depth": 20,
        "system_ndcg": 0.9064,
        "oracle_ndcg": 0.9787,
        "best_blind": "id_order",
        "best_blind_ndcg": 0.8761,
        "margin": 0.0303,
        "required": 0.05,
        "clears": false,
        "per_query": "3W 3L 1T"
      }
    ],
    "findings": [
      "The system fails the declared margin at every depth. The margin was fixed at 0.05 on 2026-08-08 before any judgement existed and has NOT been revised; a threshold changed after seeing the number it must pass is not a threshold.",
      "The mean understated the problem. Per query at depth 10 the record against id_order is 3 wins, 3 losses, 1 tie - a coin flip. The positive mean comes from one query winning by +0.3569 while three losses were smaller. On this label set there is NO evidence the ranking beats sorting by sha256 hash.",
      "id_order - sorting by content hash, relevance-blind and perfectly deterministic - scores 0.8761 nDCG@10. It is the counter-example the control was chosen to provide: determinism is not a quality property, and here it is nearly as good as the ranker on the metric.",
      "The advisory layer found exactly ONE relevant document that lexical retrieval missed, across 7 queries. Of 21 stored-vector proposals, 18 were graded irrelevant, 2 marginal, 1 relevant. Recall of D alone is already 0.9714 at depth 10, so there was almost no room to contribute.",
      "Small-sample saturation is severe. Candidate sets run 1 to 7 documents; q08 has |D| = 1, where every ranker ties trivially. nDCG's log discount is gentle enough that even a poor ordering of a mostly-relevant set scores above 0.77."
    ],
    "what_this_does_not_show": "That the ranking is bad. 7 queries and a 3-3 split is absence of evidence, not evidence of absence - the same data is consistent with a real but small effect. What it does show is that the current evidence CANNOT support a quality claim, which is the outcome the harness was built to make unavoidable.",
    "statistical_verdict": {
      "producer": "mud_detection.stats (NLKE-mud-detection), applied to the same rankings",
      "measured": "2026-08-08",
      "n": 7,
      "resolution": "1/7 = 0.143",
      "min_detectable_flips": 6,
      "discordant_system_only": 3,
      "discordant_control_only": 3,
      "mcnemar_exact_p": "1.0000",
      "system_win_rate_wilson_95": "3/7, [0.158, 0.750]",
      "control_win_rate_wilson_95": "3/7, [0.158, 0.750]",
      "intervals_overlap": true,
      "paired_delta_ci": "+0.0000, [-0.7143, +0.7143], includes zero",
      "verdict": "NOT_ESTABLISHED. At n=7 at least 6 one-directional flips are required to clear alpha=0.05; there were 3-3. McNemar exact p = 1.0000 - as un-significant as the test can report.",
      "self_correction": "The figures recorded above (system nDCG 0.9064, margin +0.0303) are quoted to four decimals on a sample whose resolution is 0.143. That is spurious precision. They are the arithmetic the producer emits and are kept for reproducibility, but the DIFFERENCE between system and control is not resolvable at this sample size and must not be read as one.",
      "why_it_matters": "The declared-margin FAIL and this verdict say different things. The margin test says the system did not clear the bar. This says the sample could not have told us either way. Both are recorded because reporting only the first would imply the evidence was capable of a verdict."
    }
  },
  "graph_contribution": {
    "producer": "python3 tools/measure_graph_contribution.py --index index.db",
    "measured": "2026-08-08",
    "question": "Does the graph layer earn its place in the sort key? Deferred through all of M1 as needing labels. It does not need them.",
    "ablation": {
      "what": "The graph signal withheld entirely - the depth map is not passed - leaving the sort key and the injective tiebreak chain byte-identical, so the comparison isolates the signal.",
      "queries": 23,
      "queries_whose_output_changes": 2,
      "which": "q02 (|D|=86, first difference at rank 20) and e06 (|D|=147, first difference at rank 61). e06 is a synthetic 13-term edge case.",
      "shallowest_rank_ever_affected": 20,
      "candidate_set_ever_changes": false
    },
    "layer_2_redundancy": {
      "verdict": "REDUNDANT",
      "rho_mean_all_queries": 0.8256,
      "rho_mean_non_empty": 0.9441,
      "queries_rank_identical": "17 of 20 non-empty; the other three are q02, e06, and q08 where |D|=1 leaves rank correlation undefined",
      "caveat": "PARTLY STRUCTURAL. The graph layer is a tiebreak: it annotates depth on BM25 candidates and cannot introduce a document, so complementarity is 0 by construction rather than by measurement. Layer 2's verdict is directionally right but tautological for this class of signal. The ablation is the load-bearing evidence."
    },
    "layer_3_nuisance": {
      "verdict": "CLEAN",
      "nuisance": "node degree (incident edges, undirected; 266 nodes, max 27, mean 4.16)",
      "rho": -0.0417,
      "moved_items": 61,
      "promoted_degree_ratio": 0.5087,
      "meaning": "The graph layer is NOT ranking by connectivity. The promoted items carry roughly half the corpus-average degree - the opposite of the feared signature. This rules out the parent-boost pathology that motivated mud-detection: a signal that propagates score along edges and learns to promote well-connected items rather than relevant ones. Ruled out without any evaluation run and without any label."
    },
    "finding": "As positioned in the sort key, the graph layer is INERT above rank 19 across the entire query set. It is not harmful and not degree-riding - it simply changes nothing a user sees. At evaluation depths 1, 5 and 10 its contribution is exactly zero on all 23 queries; at depth 20 it affects one real query.",
    "why_it_is_inert_and_why_that_is_structural": "best_depth is the FOURTH component of the sort key, after bm25_q and matched_terms. It breaks only ties that survive both, and those are rare above rank 19 on this corpus. Promoting it up the key would let graph proximity override a strictly better lexical match, which the architecture forbids by design. So the layer cannot be made influential without abandoning the ordering principle - its inertness is a consequence of the guarantee, not a tuning failure.",
    "correction_to_an_earlier_measurement": "M1.6 recorded 'graph parameters are live but weak' from a sensitivity probe that nudged graph.max_depth 2->3. That measured a nudge, not the layer. Ablation is the experiment the question actually asked, and a signal can be insensitive to its parameters while doing a great deal of work - or, as here, none.",
    "attribution": "Layers 2 and 3 are from mud-detection (github.com/verbalogicproject-creator/NLKE-mud-detection, Apache-2.0). drf takes NO dependency on it: tools/measure_graph_contribution.py degrades to the ablation alone if it is absent, and nothing is vendored."
  },
  "graph_candidate_admission": {
    "producer": "python3 tools/measure_graph_contribution.py --index index.db  (cost/benefit measured with tools/drf bench sensitivity and drf eval invariance)",
    "measured": "2026-08-08",
    "what_it_is": "The M2.2 re-scope: graph-reached documents containing no query term admitted into D, scored exactly zero, ordered strictly below every lexical match. Within that tail all scores are 0 and all matched_terms are 0, so best_depth becomes the primary discriminator - the one place graph proximity actually decides an order.",
    "setting": "graph.admit_candidates, default false",
    "invariant_holds": "Yes. Across 23 queries, zero matched documents rank below an unmatched one, and the lexical prefix is preserved elementwise. Safe only because bm25.idf uses log(...+1.0), which floors idf at zero: minimum observed score is 1017536991 and there are no zero or negative scores.",
    "cost_candidate_growth": "457 -> 2267 candidates over 23 queries (4.96x). Per-query |D| grows by 16 to 137.",
    "cost_advisory_horizon": "Collapses from 16 of 23 queries having a reachable evaluation depth to 1. Admitting graph candidates authoritatively takes the tail positions the advisory layer would occupy - the two are in direct competition for the same space.",
    "benefit_measured": "ZERO. Across the 7 labelled queries it retrieves no additional relevant document, and the one relevant document outside D ('Orchestrator-Workers' for 'sub agents') is still not reached at depth 2 from 10 seeds.",
    "mud_detection_verdict": "REDUNDANT. complementarity 0.0000, rescue_rate 0.0, rho_mean 0.726. Item sets genuinely differ here, so unlike the tiebreak case this verdict is a measurement rather than a structural artefact.",
    "nuisance_verdict": "CLEAN (rho -0.017, promoted/corpus degree ratio 1.00x). Not the parent-boost pathology - simply no benefit.",
    "sensitivity": "Reorders 20 of 23 queries - the largest blast radius of any setting in the system, exceeding ranking.b's 13. Maximum disruption, zero measured gain.",
    "decision": "Implemented, tested, invariant-checked, and DEFAULT OFF. Enabling it would be the exact failure mud-detection exists to prevent: an architecturally reasonable change that wins no recall, and here it would additionally silence the advisory layer. The evidence, not the reasoning, decides the default."
  }
}
